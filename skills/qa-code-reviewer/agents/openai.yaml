interface:
  display_name: "QA Code Reviewer"
  short_description: "Performs independent code review after QA execution."
  default_prompt: "Use qa-code-reviewer to perform an independent code review of the current diff and QA artifacts."
policy:
  allow_implicit_invocation: true
